# Bartok — Money Card · 2026-06-25 AM

*First card. No prior card to delta against; this is the baseline.*

## 1. The number

No revenue feed is reachable this run, so revenue-by-stream and revenue-per-visitor cannot be measured — stated plainly rather than guessed. GA4 (`_ga4.js`: no `GA4_PROPERTY_ID`), Search Console (`_gsc.js`: empty service-account credential), MailerLite (`MAILERLITE_API_KEY` empty), and Awin (link-builder only; no dashboard API) all returned nothing usable. The one live signal is distribution.

| Stream | This cycle | Status |
|---|---|---|
| Display ads | — | not yet eligible (pre-traffic-threshold) |
| Affiliate (Amazon + Awin) | unmeasured | Amazon links live; Awin not joined; no revenue API |
| Owned digital (guides) | $0 | built + pages live, **buy buttons disabled** pending Payhip |
| Prints / POD | unmeasured | 31 print pages live (Payhip); no sales feed |
| DTC physical | — | not started (6–12mo) |
| Brand / email promos | $0 | list growing; cannot send (no MailerLite key) |

**Distribution proxy (Blotato, live):** ~80 posts published in the trailing 7 days — Pinterest 37, TikTok 30, Instagram 30, Facebook 2, LinkedIn 1 — plus 17 scheduled ahead and 3 failed. Blotato exposes no engagement metrics, so this is reach-effort, not dollars or clicks. **Trend:** baseline — the engine is publishing across four platforms; the meter measuring it is dark.

## 2. The leak we're plugging

The biggest waste isn't a rotted link — it's the **measurement blackout**. We shipped ~80 posts this week and several products with zero attributable outcomes, because every revenue/traffic credential in `.env` is empty. That breaks the core loop (ship → measure → learn → double the winner, kill the loser): right now we cannot tell a winner from a loser. Quantified: ~80 posts/week × 0 measured results. The concrete dollar leak underneath it — the two paid digital guides ($19 + $12, ~100% margin, the designated first-dollar stream) are live as pages but their buy buttons read "Available shortly," so the highest-margin stream sits at $0 by a config gap, not a traffic gap. **Fix owners:** measurement is a Cameron credential unlock; the guide activation is Cameron's Payhip step (below).

## 3. The lever we're pulling

**Turn on digital-product revenue that is already built.** Per REVENUE-MODEL sequencing ("Now→3mo: launch DIGITAL products, lowest infra, ~100% margin"), the products, sales pages, `/shop/guides/` hub, and cross-links are all live — only the Payhip product creation + URL paste blocks the first real dollar. This outranks making more pins (≈9-week runway already) or more content, because it converts work already done into cash. **Owner this cycle:** `calmoak-merchandiser` has delivered its part (hold — do not rebuild); `calmoak-email` to pre-stage (not send) one QA-passed promo for `/shop/guides/`, ready to fire the hour buttons go live, within the ≤1-per-1–2-week cadence.

## 4. Cameron's one unlock

**Switch the two paid guides live in Payhip.** Exact step (from TEAM-LOG, Merchandiser 2026-06-24): from the repo root run `node _build-product-pdfs.js` → upload each PDF to Payhip as a Digital product ($19 Home Plan, $12 Styling Pack) → copy each `payhip.com/b/XXXXX` share URL → paste both into `assets/js/guide-links.js` (the `PRODUCTS` map) → save and redeploy. Buy buttons switch on automatically. This is the cheapest next dollar we have.

## 5. Watch

- **Capture-deploy discrepancy.** The CRO note says the 13 email-capture files were staged-not-pushed; the main-session note says committed (`4b59c9f`) and pushed live. Confirm the Starter-Guide capture actually renders on the 8 journal URLs before we count on the list growing.
- **Credentials are the second unlock.** Any one of MailerLite key / GA4 / GSC ends the blackout and lets the next card carry real numbers. Holding as Watch only because Payhip is the nearer dollar.
- **Cross-platform is live** (TikTok/IG/FB/LinkedIn posting) though W26 documented "Pinterest only" — confirm this is intended and on-brand. 3 posts failed: 2 Facebook (group posting needs app install — drop or fix) and 1 TikTok (connection error).
- **No Blotato posting slots configured** on acct 7556; agents are using explicit times. Works, but configuring slots would let publishing run on next-free-slot.
